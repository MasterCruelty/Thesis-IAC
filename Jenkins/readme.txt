In questa cartella sono contenuti gli script in bash che componevano le pipeline su Jenkins.
In particolare per evitare ridondanza, ci sono solo la pipeline dedicata all'infrastruttura e la pipeline di un micro servizio tra tutti quelli presenti.
